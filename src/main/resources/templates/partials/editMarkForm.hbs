{{#if mark}}
    <label for="mark"> {{studentId.firstName}} {{studentId.lastName}} :</label>
    <input required type="text" name="mark" placeholder="mark" value="{{#mark}}{{mark}}{{/mark}}"
           pattern="^(?:[1-9]|0[1-9]|10)$">
{{/if}}